# CelebRate Landing Page

Responsive React + Bootstrap landing page built with Vite.

## Run locally

1. Open `d:\Projects\CelebRate`
2. Run `npm install`
3. Run `npm run dev`
4. Open the local URL shown in the terminal
